# 작업 규칙

- 사용자에게 하는 모든 답변은 반드시 한국어로 작성한다 (진행 상황 알림, 질문, 최종 보고 포함).
- 모든 보고는 한국어로, 휴대폰에서 읽기 쉽게, 수치와 스크린샷 중심으로 쓴다. 사용자는 컴퓨터 초보이므로 쉬운 말로 설명한다.
- 비밀키·토큰을 저장소에 넣지 않는다. 출력하지도 않는다.
- 로그인이 필요한 비공식 API는 쓰지 않는다.
- 투자 권유로 읽히는 표현은 쓰지 않고, 고지 문구를 유지한다.
- APK를 새로 설치해야 하는 변경은 시작 전에 사용자에게 먼저 알린다.
- 새 기능은 기능 플래그(docs/기능-플래그.md) 뒤에 둔다.

# 이어서 작업하기

- 프로젝트는 `stock-briefing/` 아래에 있다. `app`은 Expo 앱이고 `backend`는 Fastify 서버다.
- 사용자가 "이어서 진행해줘" 또는 "다음 순서 진행해줘"라고 하면 먼저 두 문서를 읽고, "다음 할 일"의 첫 단계부터 진행한다.
  - `stock-briefing/docs/진행상황.md`: 지금까지 한 일, 다음 할 일, 사용자 대기 항목, 작업 방법
  - `stock-briefing/docs/로드맵.md`: 승인된 단계별 목표와 완료 기준
- 단계를 하나 끝낼 때마다 `docs/진행상황.md`와 `docs/로드맵.md` 표의 상태를 갱신한다.
