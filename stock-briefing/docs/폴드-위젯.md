# 폴드8 홈 화면 위젯 크기 (3-42 3단계, 2026-09-25)

폴드 진단(docs/폴드-진단.md)의 7) 항목입니다. 위젯 4종(잔고, 브리핑, 자산 2x1, 지수·환율)을 폴드8·폴드8 울트라의 홈 화면 크기에서 계산하고 그려 봤습니다.

## 한눈에 보기

- **숫자가 잘린 곳: 0건.** 위젯 4종 × 크기 18가지(자산은 4가지) × 글자 100·130% × 다크·라이트 × 다듬은 모습 켬·끔, 모두 464장을 그려서 쟀습니다. 고치기 전에도 0건이었습니다.
- **접은 화면(바깥 화면)은 그대로입니다.** 바깥 화면과 안쪽 화면 반쪽(미러링) 크기는 고치기 전과 그림이 한 글자도 다르지 않습니다(파일 비교로 확인).
- **넓게 늘린 위젯만 바뀝니다.** 안쪽 화면에서 미러링을 끄고 5칸 이상으로 늘린 위젯만 해당하고, 다듬은 모습(`widgetPolish`)이 켜져 있을 때만 바뀝니다.
  - 잔고: 종목을 두 열로 보여 줍니다. 안쪽 6×3(780×350dp)에서 보이는 종목이 **8개 → 16개**가 됩니다. 한 열 폭이 480dp 이상이면 가격 왼쪽에 '평가금액' 칸도 붙습니다.
  - 지수·환율: 구역 안에서도 칸을 옆으로 놓습니다. 안쪽 6×2(780×230dp)에서 **6칸 → 9칸**이 되고, 큰 위젯은 값 글자가 **19 → 24sp**로 커집니다.
- **APK가 필요한 것: 2가지.** 한 위젯이 두 화면에 같은 크기로 그려지는 라이브러리 한계와, 화면마다 다른 배치입니다. 둘 다 폰에서 먼저 확인해야 합니다. 이번에는 만들지 않았습니다(아래 '라이브러리 한계').

## 1. 조사: One UI 9 홈 화면 격자

| 항목 | 알아낸 것 | 확실한 정도 |
|---|---|---|
| 폴드8 바깥 화면 격자 | 기본 **4열**. Home Up(굿락)으로 열·행을 4·5·6까지 바꿀 수 있음 | 기사 확인 (행 기본값은 기사마다 다름: 4×4 설명도 있음) |
| 폴드8 울트라 바깥 화면 | 6.5인치 FHD+, 격자 수는 공개된 자료 없음 → 4열 × 5~7행으로 추정 | 추정 |
| 안쪽 화면 격자 | 공개된 숫자 없음. 폴드6 안내는 안쪽 6×5. 폴드7부터 안쪽 격자 바꾸기가 한때 빠졌다가 Home Up으로 돌아옴 → 6~8열 × 4~7행으로 추정 | 추정 |
| 커버 화면 미러링 | **기본 켬.** 켜져 있으면 안쪽 화면의 왼쪽 반이 바깥 첫 페이지, 오른쪽 반이 둘째 페이지입니다. 한쪽에 둔 위젯이 다른 쪽에도 그대로 보입니다. 끄면 화면마다 따로 배치(위젯·격자 따로) | 삼성 안내·기사 확인 |
| 같은 위젯이 두 화면에 | 미러링을 켜면 **같은 위젯 하나**가 바깥과 안쪽 반쪽에 모두 나옵니다. 이 라이브러리는 위젯 하나를 **한 크기로만** 그립니다(아래 참고) | 확인 (라이브러리 코드) |

출처: [삼성 폴드8 안내](https://www.samsung.com/us/support/answer/ANS10013191/) · [삼성 폴드8 울트라 안내](https://www.samsung.com/us/support/answer/ANS10013184/) · [SamMobile 미러링 설명](https://www.sammobile.com/news/one-galaxy-z-fold-8-and-ultra-feature-every-new-user-should-try/) · [SammyGuru 바깥 격자](https://sammyguru.com/how-to-get-more-app-icons-on-the-galaxy-z-fold-8s-cover-screen/) · [삼성 폴드6 격자 6×5](https://insights.samsung.com/2024/08/05/how-to-customize-home-screens-on-your-galaxy-z-fold6/)

## 2. 위젯 크기 표 (dp, 추정)

화면 크기(420dpi 추정)에서 상태 표시줄 약 28, 제스처 막대 16~48, 아래 독 약 88, 페이지 점 약 16, 좌우 여백 8~16을 빼고 격자로 나눈 값입니다. **폰 실측 전이라 범위로 적었습니다.** 테스트(`test/widgetLayout.test.ts`의 `FOLD_SIZES`)는 범위의 양 끝과 가운데를 모두 봅니다. 실측 값을 받으면 이 표와 `FOLD_SIZES`만 고치면 됩니다.

| 자리 | 화면 크기 | 한 칸(폭 × 높이) | 4×2 | 4×3 | 4×4 | 6×2 · 6×3 · 6×4 |
|---|---|---|---|---|---|---|
| 폴드8 바깥 (4열) | 475×751 | 107~115 × 95~150 | 430~460 × 180~290 | × 280~440 | × 370~590 | - |
| 울트라 바깥 (4열) | 411×960 | 95~100 × 110~160 | 380~400 × 210~320 | × 320~480 | × 430~640 | - |
| 안쪽 반쪽 (미러링 켬) | 폴드8 466×704 · 울트라 429×954 | 바깥과 비슷 | 395~450 × 165~320 | - | × 340~630 | - |
| 폴드8 안쪽 가로 (미러링 끔, 6~8열) | 933×704 | 112~150 × 104~135 | 440~600 × 200~270 | - | - | 670~900 × 200~270 · 300~400 · 400~540 |
| 울트라 안쪽 세로 (미러링 끔, 6~8열) | 859×954 | 103~138 × 113~158 | 400~550 × 215~316 | - | - | 610~830 × 215~316 · 330~470 · 440~630 |
| 자산 2×1 | 바깥 · 안쪽 | - | 폴드8 205~230 × 85~145 · 울트라 185~200 × 100~160 · 안쪽 200~420 × 80~160 | | | |

## 3. 점검 결과 (고치기 전)

- **글자 폭 어림 계산:** 폭 380~920 × 높이 160~640 × 글자 90~130%, 1만 건 넘게 계산했습니다. 넘침 0건.
- **그려서 재기:** 위젯 트리를 라이브러리 빌더로 만들고 크롬에서 재어 봤습니다. 잘린 글자와 칸 밖 글자 0건(464장). 이름과 브리핑 문장의 '…' 줄임은 원래 규칙대로라 뺐습니다.
- **빈 공간** (고칠 것은 아니고, 기록만 해 둡니다):
  - 지수·환율(바깥 화면 큰 크기): 울트라 4×4(390×540)는 아래가 **206dp** 비고, 폴드8 4×4(445×480)는 146dp 빕니다. 9칸을 다 보여 준 뒤 남는 높이입니다. 접은 화면이라 이번에는 그대로 두었습니다.
  - 브리핑: 늘 3종목까지만 보여서 큰 위젯은 아래가 많이 빕니다. 울트라 바깥 4×4는 396dp, 안쪽 6×4는 300~396dp입니다. 서버가 3종목만 주므로 더 보이려면 서버와 앱을 함께 바꿔야 합니다. 다음 단계 후보입니다.
  - 잔고와 지수·환율(안쪽 넓은 크기): 한 줄에 종목 하나만 있어서 가운데가 400dp 넘게 비었습니다(전 캡처 참고). **이번에 고쳤습니다.**
  - 자산: 문제 없음(아래 빈 곳 24~49dp).

## 4. 고친 것 (다듬은 모습 `widgetPolish` 켬일 때만, OTA)

기준 폭은 모두 토큰입니다. `app/src/widgets/layout.ts`의 `WIDE`와 `palette.ts`의 `WIDGET_BOARD.value.wideMax`에 있습니다.

| 토큰 | 값 | 뜻 |
|---|---|---|
| `WIDE.valueMin` | 480 | 잔고 한 열이 이 폭(dp) 이상이면 평가금액 칸 (위젯 폭 약 504dp 이상) |
| `WIDE.columnMin` | 300 | 잔고 한 열의 최소 폭. 두 열은 위젯 폭 약 644dp 이상 |
| `WIDE.columnGap` | 20 (`space.xl`) | 두 열 사이 |
| `WIDE.boardMin` | 540 | 지수·환율이 구역 안에서 칸을 옆으로 놓을 수 있는 위젯 폭 |
| `WIDE.tileGap` | 12 (`space.md`) | 구역 안 옆 칸 사이 |
| `WIDGET_BOARD.value.wideMax` | 24sp (`WIDGET_FONT.huge`) | 넓은 지수·환율 위젯의 값 글자 상한 (좁은 위젯은 19sp 그대로) |

**바깥 화면(380~460dp)과 안쪽 반쪽(395~450dp)은 모두 이 기준보다 좁습니다.** 그래서 접은 화면과 미러링 화면은 지금과 똑같습니다.

| 크기 (글자 100%) | 잔고: 다 보이는 종목 | 지수·환율: 칸 · 값 글자 |
|---|---|---|
| 바깥·반쪽 모든 크기 | 그대로 | 그대로 |
| 안쪽 4×2 (미러링 끔, 520×230) | 4 → 4 (평가금액 칸 추가) | 6 → 6 (그대로) |
| 안쪽 5×2 (650×230) | 4 → **8** | 6 → **9** · 19 → 23sp |
| 안쪽 6×2 (780×230) | 4 → **8** | 6 → **9** · 19 → 24sp |
| 안쪽 6×3 (780×350) | 8 → **16** | 9 → 9 · 19 → **24sp** |
| 안쪽 6×4 (900×470) | 12 → **18 (전부)** | 9 → 9 · 19 → **24sp** (아래 빈 곳 136 → 91dp) |
| 울트라 안쪽 6×2 (720×265) | 5 → **10** | 9 → 9 · 19 → 24sp |
| 울트라 안쪽 6×4 (830×540) | 14 → **18 (전부)** | 9 → 9 · 19 → 24sp (아래 빈 곳 206 → 125dp) |

글자 130%에서도 같은 방향입니다(예: 6×3 잔고 6 → 12종목). 지수·환율 5×2의 130%만 값이 19 → 17sp로 작아집니다. 대신 6칸 → 9칸이 되고, 값이 12sp(comfort) 아래로는 내려가지 않게 막았습니다.

- **잔고 두 열:** 한 줄에 종목 두 개를 놓고, 순서는 한 열일 때와 같습니다(왼쪽 → 오른쪽, 평가금액 큰 순). 칸마다 따로 눌러 그 종목 상세를 열고, 화면 읽기도 칸마다 따로 합니다. 줄 높이(약 38dp)는 지금과 같습니다.
- **평가금액 칸:** 작은 회색 '평가금액' 아래에 금액을 둡니다. 손익 줄과 같은 통화이고 기본은 원화입니다. 화면 읽기에도 '평가금액 10,116,000원'을 붙입니다. 관심 종목은 빈 칸입니다. 칸을 넣었을 때 손익 줄이 짧아지거나 가격·등락률이 두 줄로 쌓이면, 칸을 빼고 지금 모습으로 둡니다.
- **지수·환율 옆 칸:** 줄 수(4 → 1)마다 구역의 옆 칸 수를 계산해 점수가 가장 높은 배치를 고릅니다. 예를 들어 2줄이면 국내 1 · 미국 2 · 환율 2입니다. 점수가 같으면 지금처럼 세로 배치를 고릅니다. 좁은 배치보다 칸이 줄거나 값이 작아지면 쓰지 않습니다. 숫자는 자르지 않고, 이름도 줄이지 않습니다.
- **다듬은 모습을 끄면** 넓은 크기도 예전 모습 그대로입니다. 지수·환율 위젯에도 `widgetPolish`를 넘기도록 `render.tsx`를 고쳤습니다.
- 기능 플래그: 새 넓은 화면 기능은 원래 `foldLayout` 뒤에 둡니다. 하지만 위젯은 `/api/widget`의 `features`로만 플래그를 받고, 그 안에 `foldLayout`이 없습니다. 그래서 이번 단계 지시대로 기존 `widgetPolish` 뒤에 두었습니다. 넓은 위젯만 따로 끄려면 서버 위젯 응답에 플래그를 하나 더 넣어야 합니다(서버 + OTA). `docs/기능-플래그.md`의 `widgetPolish` 줄에 "끄면 넓은 위젯의 두 열·평가금액 칸·지수 옆 칸도 없음"을 한 줄 더하면 좋습니다(이번에는 문서 규칙상 손대지 않음).

## 5. 라이브러리 한계 (react-native-android-widget 0.22.1)

1. **위젯 하나 = 한 크기.** 위젯 크기는 `RNWidgetUtil.java`가 정합니다. 앱 쪽 방향이 세로면 `MIN_WIDTH × MAX_HEIGHT`, 가로면 `MAX_WIDTH × MIN_HEIGHT` 한 벌만 골라 그 크기의 그림 한 장을 만듭니다. 안드로이드 12의 크기별 레이아웃(`OPTION_APPWIDGET_SIZES`)은 쓰지 않습니다.
2. **그림은 늘이지 않고 왼쪽 위에 붙습니다** (`rn_widget.xml`, `scaleType="matrix"`). 실제 칸이 그림보다 크면 오른쪽·아래가 투명하게 비고, 작으면 **오른쪽·아래가 잘립니다.**
3. 그래서 미러링을 켜면 같은 위젯이 바깥(예: 460×290)과 안쪽 반쪽(예: 435×200)에 나오는데, 그림은 한 크기뿐입니다. 폴드8을 펼치면 기본 자세가 가로라서 `MAX_WIDTH × MIN_HEIGHT`로 그립니다. 이때 삼성 런처가 두 화면 중 넓은 쪽 폭을 준다면 안쪽 반쪽에서 **오른쪽 10~30dp(↻, 등락률)가 잘릴 수 있습니다.** 삼성 런처가 두 화면 크기를 어떻게 넣는지는 폰에서만 알 수 있습니다.
4. 접고 펼 때 크기 변경 알림(`WIDGET_RESIZED`)이 오면 그 화면 크기로 다시 그립니다(`widgetTaskHandler.tsx`가 이미 처리). 오지 않으면 다음 갱신(30분 주기, 앱 열기, ↻) 전까지 다른 화면 크기의 그림이 남습니다.
5. JS로는 '다른 화면의 크기'를 알 수 없습니다. 그래서 이번에 JS로 고칠 수 있는 잘림은 없었습니다(계산과 그림 모두 0건).

## 6. APK가 필요한 것 (만들지 않음 → 8) 1.5.0에 묶을 후보)

| 후보 | 왜 APK인가 | 먼저 폰에서 볼 것 |
|---|---|---|
| ① 화면(크기)마다 다른 그림 | 라이브러리 자바 코드를 고쳐 `RemoteViews(Map<SizeF, RemoteViews>)`로 크기별 그림을 넘겨야 함 (patch-package + 네이티브 빌드) | 미러링을 켜고 펼쳤을 때 오른쪽·아래가 잘리는지, 빈 띠가 생기는지 |
| ② 그림을 칸에 맞게 늘이기·가운데 두기 | `rn_widget.xml`의 `scaleType` 변경 (네이티브 리소스) | ①과 같음. ①이 필요 없을 만큼 차이가 작으면 이것만으로 충분 |
| ③ 위젯 기본 크기·최소 크기 바꾸기 (예: 자산 2×1 최소 높이) | `app.json` 위젯 설정 | 바깥 화면에서 위젯을 놓을 때 기본 칸 수가 알맞은지 |

JS만으로 되는 후보는 폰 결과를 보고 정합니다. 예를 들어 `screenInfo`의 화면 폭을 보고 여백을 두는 방법이 있습니다. 근거 없이 넣으면 일반 폰의 가로 화면까지 바뀔 수 있어 이번에는 넣지 않았습니다.

## 7. 폰에서 확인할 것 (사용자 약 10분)

1. 바깥 화면과 안쪽 화면에서 홈 화면을 길게 누르고 설정 → 홈 화면 격자의 숫자(열 × 행)를 캡처합니다.
2. 미러링 켬 상태에서 잔고와 지수·환율 위젯을 4×2로 놓습니다. 접었을 때와 펼쳤을 때 오른쪽·아래가 잘리는지 캡처합니다. 펼친 뒤 30초 안에 바뀌는지도 봅니다.
3. 미러링 끔 상태에서 안쪽 화면에 잔고와 지수·환율 위젯을 6×2와 6×3으로 늘려 캡처합니다(두 열과 9칸이 보이는지).
4. 설정 → 화면 정보(1단계 기능)의 공유 글을 보냅니다. 폭과 밀도를 알면 표 2의 범위를 한 값으로 바꿀 수 있습니다.

## 8. 캡처 (미리보기, 다크 · 글자 100% · 다듬은 모습 켬)

위젯 코드가 만든 트리를 그대로 HTML로 옮겨 크롬으로 캡처했습니다(1 CSS px = 1 dp). 실제 폰과 글꼴은 조금 다를 수 있습니다. 파일 위치: `scratchpad/shots/fold-step3/widgets/`

- `비교.png`: 전·후 한 장 요약
- `잔고-바깥-460x290-{before,after}.png` · `지수-바깥-460x290-{before,after}.png`: 바깥 화면(같음)
- `잔고-안쪽-780x350-{before,after}.png` · `지수-안쪽-780x350-{before,after}.png`: 안쪽 6×3
- `잔고-안쪽-780x230-*` · `지수-안쪽-780x230-*`: 안쪽 6×2, `잔고-안쪽-560x300-*`: 평가금액 칸

## 9. 테스트

- `test/widgetLayout.test.ts` '폴드8·울트라 크기 (3-42)' 12개 항목을 봅니다.
  - 크기 표 전체에서 숫자 잘림 0 · 넘침 0 (위젯 4종 × 100·130% × 다듬은 모습 켬·끔)
  - 폭 380~920 × 높이 160~640 × 글자 90~130% 전수
  - 바깥·반쪽은 배치가 지금과 똑같음
  - 넓은 잔고는 두 열·평가금액 칸
  - 넓은 지수·환율은 9칸이고, 좁은 배치보다 나빠지지 않음
- `test/widgetRender.test.tsx` '폴드8·울트라 크기 (3-42 폴드 3단계)' 5개 항목을 봅니다.
  - 라이브러리 트리 빌더로 폴드 크기 × 라이트·다크 × 100·130% × 켬·끔을 그림
  - 두 열의 칸마다 누르는 곳과 읽는 말
  - 평가금액을 읽는지
  - 끄면 6칸·한 열

## 10. 위젯 2차 (2026-09-26): 폴드 두 화면에 맞게 · 실패 뒤 15분 안에 다시

사용자 캡처(2026-09-26 토 10:32, 커버 화면 미러링 켬) 두 장에서 나온 두 가지를 고쳤습니다. **OTA 만(runtime 1.4.0), 새 APK 필요 없음.** 서버는 플래그 한 줄만 더합니다.

### 캡처로 잰 크기

글자·줄 높이로 거꾸로 재면 이 폰은 **1dp ≈ 2.1px** 입니다(문서의 420dpi 추정은 2.625). 그래서 위젯 칸이 추정보다 넓습니다.

| 자리 | 캡처(px) | dp (÷2.1) |
|---|---|---|
| 바깥 4x2 (잔고·지수 같음) | 1065×467 | **약 507×222** |
| 안쪽 잔고 | 999×1283 | 약 476×611 |
| 안쪽 지수·환율 | 998×737 | 약 476×351 |

바깥 칸 폭(507dp)이 잔고 평가금액 칸 기준(504dp)을 넘습니다. 추정(430~460dp)으로는 넘지 않아 '접은 화면은 그대로'라고 적었던 것이 틀렸습니다. **'화면 정보' 공유 글로 밀도를 확인해 주시면 표 2를 한 값으로 고칩니다.**

### 접고 펼 때 실제로 일어나는 일 (라이브러리 코드)

- 위젯 그림은 **한 장**입니다(라이트·다크 한 벌). 그리는 순간 폰 방향으로 크기를 고릅니다(`RNWidgetUtil`): 세로(접힘·바깥 화면)면 '가장 좁은 폭 × 가장 큰 높이', 가로(펼침·안쪽 화면)면 '가장 넓은 폭 × 가장 작은 높이'. 폭·높이 후보는 런처가 준 범위(`OPTION_APPWIDGET_MIN/MAX_*`)입니다.
- 그림은 늘이지 않고 왼쪽 위에 붙습니다(`rn_widget.xml` `scaleType="matrix"`). 칸보다 크면 오른쪽·아래가 잘리고, 작으면 빈 곳이 남습니다. 누르는 곳(↻·종목 줄)도 그 그림 기준 자리입니다.
- 다시 그리는 때: 위젯 주기 갱신(30분), 백그라운드 작업(15분), 앱, ↻, 그리고 **런처가 위젯 옵션을 보낼 때**(`onAppWidgetOptionsChanged`). 옵션이 오면 라이브러리는 '지금 방향으로 고른 크기'를 '마지막으로 적어 둔 크기'와 비교해 다르면 `WIDGET_RESIZED` 로 곧 다시 그립니다(`RNWidgetProvider.isSizeChanged`).
- JS 가 받는 것: 그 순간의 폭·높이(`widgetInfo`)와 화면 크기(`screenInfo`)뿐. 다른 화면의 칸 크기는 모릅니다.

삼성 런처가 크기를 알려 주는 방식에 따라 셋으로 나뉩니다.

| 경우 | 미러링 위젯(같은 위젯이 두 화면)에 일어나는 일 | 고친 뒤 |
|---|---|---|
| ② **화면마다** — 접고 펼 때 그 화면 칸 크기로 옵션을 바꿔 보냄 | 접고 펼 때마다 `WIDGET_RESIZED` 로 그 화면 크기로 다시 그림. 바깥 칸이 507dp 라 **평가금액 칸이 나옴**(캡처와 같음) | 크기는 그대로, 넓은 모습(평가금액 칸·두 열·지수 옆 칸)은 **본 칸 중 가장 좁은 폭(476)** 이 허락할 때만 → 바깥에 평가금액 칸 없음. **안쪽은 그대로(한 열 목록 — 캡처 13줄)** |
| ① **범위** — 두 화면 칸 크기를 모두 범위로, 접어도 같은 옵션 | 접고 펴도 다시 그리지 않아, 한 그림이 두 화면에 번갈아 보임. 접힌 채 그리면 안쪽 모양(476×611), 펼친 채 그리면 바깥 모양(507×222) — **늘 반대 화면 모양** | 두 화면에 모두 들어가는 **476×222 카드**, 남는 곳은 투명. 대신 안쪽 잔고 칸(476×611)은 **칸의 36% 만 카드(약 4줄)**, 지수는 63% |
| ③ **범위 + 다시 보냄** — 범위는 같은데 접을 때 옵션을 한 번 더 보냄 | 방향이 바뀌어 '고른 크기'가 달라지므로 `WIDGET_RESIZED` 로 **곧 반대 화면 모양으로 다시 그림**(접으면 476×611 이 507×222 칸에 — 아래가 잘림) | ②와 가릴 수 없어 ②로 봄 → 고치기 전과 같음(남은 한계). 한 화면을 돌려 보면 ①로 알아봄 |

**캡처 두 장은 ②를 가리킵니다.** 같은 10:32 에 두 화면의 잔고 그림이 서로 다르고(바깥은 507×222 모양 + 평가금액 칸, 안쪽은 476×611 한 열 13줄) 각자 제 칸에 딱 맞습니다. ①이라면 두 화면이 같은 그림이어야 하고, ③이라면 접을 때마다 반대 화면 모양이어야 합니다. 지수·환율 위젯만 두 화면 모두 '갱신 실패'인 것도, 접을 때 온 `WIDGET_RESIZED` 에서 잔고는 받아 둔 응답을 다시 쓰고 지수는 판을 새로 묻다 실패한 것으로 설명됩니다. 그래서 **폰에서 기대할 모습: 바깥 화면에서 평가금액 칸이 사라지고, 안쪽 화면은 지금처럼 한 열 목록**입니다. ①·③은 다른 설정·다른 런처일 때를 위해 함께 막았습니다.

- **안쪽만 쓰는 위젯(미러링 끔)**: 바깥에는 안 보입니다. 접혀 있을 때 주기 갱신이 오면 세로 크기로 그려, 펼치면 다음 갱신까지 그 그림이 보입니다. 이때 두 방향 크기가 ①처럼 엇갈리면 두 방향에 들어가는 카드로 그립니다. 펼친 화면에서만 그렸으면 지금 그대로(3-42 넓은 모습 그대로).
- **일반 폰을 돌린 것**: 같은 화면이라 지금 그림 그대로입니다. 가로에서 화면의 짧은 변이 상태 표시줄·아래 막대만큼(10~20%) 짧아져도 같은 화면으로 봅니다(기준 25%, 폴드 바깥↔안쪽은 30% 넘게 다름).
- **앱이 떠 있을 때 접거나 펴면**: 1초 뒤 위젯 4종을 마지막 값으로 다시 그립니다(서버 호출 없음). 앱이 닫혀 있으면 ②는 런처 알림으로 곧, ①은 다음 갱신 때 바뀝니다.

### 앱이 가리는 방법 (`app/src/widgets/frame.ts`)

위젯마다 **'어느 화면(화면의 짧은 변)·어느 방향에서 어떤 크기를 봤는지'** 를 화면·방향마다 한 줄씩(최대 4줄) 기기에 적습니다(`widget.frame.<번호>`, 서버로 보내지 않음). 두 화면에서 본 적이 있으면:

1. 두 화면에서 같은 방향으로 **같은 크기** → ① (확실).
2. 두 화면에서 같은 방향으로 **다른 크기**, 또는 세로 크기가 가로 크기보다 넓거나 낮음(①이라면 나올 수 없는 모양 — 캡처 크기가 이것) → ② (확실).
3. 둘 다 아니면: 크기 변경 알림이 **기억과 같은 크기로** 온 적이 있으면(접고 펼 때 온 알림) ②, 없으면 ①로 보고 두 화면에 들어가게 그립니다. 바깥 칸이 좁고 높은 ②(예: 바깥 445×360 · 안쪽 780×350)는 첫 펼침 한 번은 작은 카드(칸의 57%)였다가, 다시 접을 때 알아보고 제 크기로 돌아옵니다(`지수-화면마다-좁고높은바깥-전후.png`).

같은 화면·방향에서 크기가 바뀌면(크기 조절·격자·화면 확대) 나머지 기억을 모두 버리고, 14일 동안 못 본 줄도 버리며, 위젯을 지우면 기억도 지웁니다. 예전 방식(방향만으로 기억)은 바깥 화면을 가로로 돌린 채 한 번만 그려도 영영 ①을 잊거나, 안쪽 화면을 세로로 돌리면 바깥 기억을 덮어썼는데(검증 지적) 이제는 그렇지 않습니다(테스트로 50번 접고 펴기까지 확인).

코드: `frame.ts`(기억·가리기·배치 크기), `render.tsx` `renderFor`(모든 그리기가 이것을 씀 — 태스크 핸들러·앱 즉시 갱신·다시 그리기, 시각은 그리는 시각), `widgetTaskHandler.tsx`(크기 변경 알림 표시), `components/WidgetBridge.tsx`(화면 크기 변경), 플래그 `widgetFoldFit`(서버 기본 켜짐, 앱 fallback 꺼짐). **플래그를 끄면 지금 그림 그대로**입니다 — ①에서 안쪽 화면이 너무 비어 불편하면 서버에서 끄면 됩니다.

### 실패 뒤 다시 묻기 (지수·환율 위젯만 '갱신 실패 · 연결 안 됨')

- 원인: 휴장·주말에는 받아 둔 응답을 2시간까지 다시 씁니다(3-16). 한 위젯이 잠깐 실패하면(예: 판을 새로 물어야 하는 지수·환율 위젯만 서버에 물음) 다음 백그라운드 작업은 건너뛰어 그 실패가 **최대 2시간** 남았습니다.
- 고친 것: 어느 갱신이든(백그라운드·주기·크기 변경·추가·↻) 서버 조회에 실패하면 그 조회를 다 마친 뒤 기기에 '실패 표시'(`widget.retry`)를 적습니다. 표시가 있으면 **다음 백그라운드 작업(약 15분, 안드로이드가 미룰 수 있음)이 장 상태와 상관없이 다시 묻습니다.** 받는 데 성공하면 표시를 지우고 **위젯 4종을 모두 다시 그려 '갱신 실패'를 지웁니다.** 그 뒤로는 휴장 2시간 재사용 그대로(서버 호출이 늘지 않음).
- 위젯이 스스로 하는 갱신(주기·크기 변경·추가)은 실패 표시가 있어도 **예전처럼 받아 둔 응답을 다시 쓸 수 있으면 씁니다.** 그래서 접고 펼 때 위젯 4종이 한꺼번에 서버에 묻지 않고, 연결이 나쁠 때 멀쩡한 위젯까지 '갱신 실패'로 바뀌지 않습니다(검증 지적).
- 여러 조회가 한꺼번에 돌 때: 성공한 조회는 **자기가 시작하기 전에 적힌 표시만** 지우고, 지운 조회 하나만 위젯 4종을 다시 그립니다(예전: 성공마다 4종 — 최대 16번). 성공한 조회가 도는 사이 다른 위젯이 실패하면 표시가 남아 다음 백그라운드 작업이 다시 묻고 지웁니다.
- 테스트 `app/test/widgetRetry.test.tsx`: 토 10:00 실패 → 10:15 다시 묻고 성공, 10:30 은 다시 건너뜀, 실패 뒤 접고 펴도 서버 호출 0·실패가 번지지 않음, 동시 성공 3건에 다시 그리기 한 번, 성공 도중 실패하면 표시가 남음.

### 남은 한계

- ① 경우 안쪽 화면에서 카드가 칸보다 작습니다(잔고 476×611 칸에 476×222 카드 — **약 4줄, 칸의 36%**. 지수 63%). 그림이 한 장이라 두 화면을 다 맞추려면 작은 쪽에 맞출 수밖에 없습니다. APK 1.5.0 전까지 이렇고, 불편하면 `widgetFoldFit` 을 끄면 예전 그림으로 돌아갑니다(대신 반대 화면 모양·잘림이 다시 생김).
- 바깥 칸(507dp) 하나만으로는 평가금액 칸 기준(504dp)을 넘습니다. 그래서 **업데이트 뒤 처음 접고 펴기 전**, 그리고 **미러링된 안쪽 칸을 504dp 이상으로 넓히면** 바깥 화면에 평가금액 칸이 다시 나옵니다(가장 좁은 칸이 허락하므로).
- ②인데 바깥 칸이 좁고 높은 모양이면 첫 펼침 한 번은 작은 카드입니다(위 3번). ③ 런처는 ②로 보여 고치기 전과 같습니다.
- 두 크기를 보는 화면의 짧은 변 차이가 25% 이하면 같은 화면으로 봅니다(폴드8 은 약 32%, 일반 폰을 돌리면 10~20%). 폴드가 아닌 기기·다른 폴드에서 이 차이가 25% 근처면 잘못 가릴 수 있습니다.
- 앱이 닫혀 있고 ①이면 접고 펴도 다음 갱신(최대 15~30분)까지 옛 그림입니다.

### APK 1.5.0 에서 고칠 방법 (만들지 않음 — APK 전에 먼저 알려 드림)

**진짜 해결: 크기마다 그림을 따로 넘기기 (안드로이드 12 이상).**
- 라이브러리 자바 코드를 `patch-package` 로 고칩니다.
  - `RNWidgetUtil`: `getAppWidgetOptions(id).getParcelableArrayList(OPTION_APPWIDGET_SIZES)`(API 31+)로 런처가 준 크기 목록(`SizeF`, dp)을 읽어 `widgetInfo.sizes` 로 JS 에 넘김.
  - `RNWidget.drawWidget`: 크기마다 라이트·다크 그림을 만들어 파일 이름에 크기를 넣어 저장(`saveBitmapToDisk(id, "light-476x611")` 식), 누르는 곳·목록(`RNWidgetCollectionService` 는 위젯 번호 대신 '번호+크기'로 저장)도 크기마다 만든 뒤 `new RemoteViews(Map<SizeF, RemoteViews>)` 로 `updateAppWidget`. 목록이 없거나 API 30 이하면 지금처럼 한 장.
  - JS: `renderWidget` 이 크기마다 한 벌씩 그림을 돌려주도록(`{ "476x611": {light, dark}, ... }`) 태스크 핸들러·`requestWidgetUpdate` 를 바꾸고, 이렇게 되면 `widgetFoldFit` 은 끕니다.
- 좋은 점: 런처가 화면마다 맞는 그림을 스스로 고릅니다(접고 펴도 다시 그릴 필요 없음). 두 화면 모두 그 칸에 딱 맞는 모습(안쪽 한 열 목록 전체·바깥 4줄), 투명한 빈 곳·잘림 없음. ①②③ 모두 해결.
- 나쁜 점: 네이티브 수정이라 **새 APK 설치**가 필요합니다. 크기마다 그림·누르는 곳·목록 그림이 늘어 그리는 시간과 저장 공간이 2~4배. 라이브러리를 올릴 때마다 패치를 다시 맞춰야 합니다. 삼성 런처가 크기 목록을 실제로 주는지 폰에서 먼저 확인해야 합니다(`dumpsys appwidget` 의 options).

**더 작은 대안: `rn_widget.xml` 의 `scaleType` 을 `fitStart` 로.** 그림을 칸에 맞춰 비율을 지키며 **줄이거나 늘립니다**(작은 그림도 커짐). 잘림은 없어지지만 글자 크기가 칸마다 달라지고 흐려질 수 있어 권하지 않습니다(위 방법과 함께 안전장치로만).

**권하지 않는 대안: 앱이 화면 변경을 듣고 다시 그리기(네이티브 `ComponentCallbacks`).** 앱이 살아 있을 때만 되고 ①에서는 여전히 한 그림이라 해결이 아닙니다(지금 JS 로 한 것과 같은 효과).

### 캡처 (미리보기, 다크 · 글자 100% · 다듬은 모습 켬)

`scratchpad/shots/widget-fold/`: `요약-전후.png`(휴대폰용 한 장 — 캡처와 같은 ② 경우), `잔고-화면마다-전후.png`·`지수-화면마다-전후.png`(②), `지수-화면마다-좁고높은바깥-전후.png`(② 바깥 칸이 좁고 높을 때 — 첫 펼침·다시 편 뒤), `잔고-범위런처-전후.png`·`지수-범위런처-전후.png`(① 네 가지), `잔고-추정크기-전후.png`(420dpi 추정 크기 ① — ↻ 잘림). 줄무늬 = 홈 화면 바탕(위젯 칸), 점선 = 칸 경계. 카드가 칸보다 작으면 '칸의 몇 %' 를 적었습니다.

### 폰에서 확인할 것 (사용자 약 5분)

1. 앱을 한 번 열었다 닫은 뒤, 폰을 접었다 펴기를 두 번 합니다(두 화면 크기를 기억하게).
2. 접었을 때와 펼쳤을 때 잔고·지수 위젯을 캡처합니다. 바깥 화면에 평가금액 칸이 없고, 안쪽은 지금처럼 한 열 목록이면 됩니다. 안쪽이 위쪽 4줄만 보이고 아래가 비면 ① 경우이니 알려 주세요.
3. 와이파이를 잠깐 끄고 지수 위젯의 ↻ 를 눌러 '갱신 실패'를 띄운 뒤 와이파이를 켜고, 15~20분 안에 모든 위젯에서 '갱신 실패'가 사라지는지 봅니다(설정의 '마지막 자동 갱신' 시각도).
4. 설정 > '화면 정보' 공유 글(접은 화면·펼친 가로)을 보내 주세요. 밀도가 2.1 근처인지 확인합니다.
