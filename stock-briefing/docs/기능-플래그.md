# 기능 켜고 끄기 (기능 플래그)

새 기능을 재배포 없이 켜고 끕니다. 문제가 생기면 끄고, 준비되면 켭니다.

## 규칙

- **플래그 목록은 서버 한 곳**: `backend/src/services/featureService.ts` 의 `FEATURES`. 앱에는 목록이 없고 `/api/features` 로 받습니다.
- **기능 하나에 플래그 하나.** 조합이 복잡해지지 않게, 완전히 켠 뒤 한 달이 지나면 플래그를 지우고 코드에 합칩니다.
- **공통 마무리 규칙**: 3-19·3-24 의 새 동작과 P2 기능은 모두 플래그 뒤에 둡니다. 끄면 그 기능의 화면·알림·서버 작업이 0건이어야 하고, 이를 테스트로 확인합니다.
- 결제·가격 관련 코드는 넣지 않습니다(유료화는 나중에 이 구조 위에 따로 결정).

## 지금 있는 플래그

| 키 | 기본 | 끄면 |
|---|---|---|
| `tossReconcile` | 켜짐 | 동기화 때 토스 대조(시세 조회·기록·경고)를 하지 않고, `/health`·토스 상태에서 대조 값이 빠져 설정 화면의 '토스 대조' 줄이 사라짐 (3-13) |
| `briefingSources` | 켜짐 | 브리핑 상세의 근거(뉴스·공시 출처) 카드가 사라짐 (3-12) |
| `briefingDigest` | 켜짐 | 브리핑 알림이 예전처럼 종목마다 1건. 조용한 시간·종목별 끄기도 적용하지 않고 설정 화면에서 그 항목이 사라짐 (3-19, 서버 푸시와 앱 백그라운드 알림 모두) |
| `briefingTabMovers` | 켜짐 | 브리핑 탭이 등록순, '변동 큰 종목' 카드와 정렬 선택이 사라짐 (3-19) |
| `briefingManualRun` | 켜짐 | 수동 생성이 확인 없이 바로 시작, 상세의 '이 종목 다시 만들기' 버튼이 사라짐 (3-19) |
| `widgetPnlToggle` | 켜짐 | 잔고 위젯 합계 옆 손익을 눌러도 누적·당일이 바뀌지 않음. 예전처럼 누적만 보이고 누르면 앱이 열림 (위젯 요청) |
| `widgetIndexLine` | 켜짐 | 잔고 위젯 합계 아래 코스피·나스닥·환율 줄이 사라지고, `/api/widget` 응답에 지수를 넣지 않음(지수 조회 0건) (위젯 요청). 켜져 있어도 지수는 새 앱이 `?indices=1` 로 물을 때만 넣음 |
| `widgetMarket` | 켜짐 | 지수·환율 위젯(APK 1.4.0)에 숫자 대신 짧은 안내("지금은 지수·환율을 표시할 수 없습니다")만 보이고, `/api/widget` 응답에 지수 판을 넣지 않음(판만 물은 요청은 지수 조회 0건). 켜져 있어도 판은 지수·환율 위젯이 있는 1.4.0 앱이 `?board=1` 로 물을 때만 넣음 |
| `widgetPolish` | 켜짐 | 잔고 위젯이 다듬기 전 모습 그대로: 칩은 한 개('미국 주간거래'), 제목 '잔고 18', 지수 줄은 코스피·나스닥·환율(받은 순서, 지난 세션 값도 등락색), 종목 줄 '+12.34% +$1,234'(원화 표시 설정을 따름)·오른쪽 등락률만·누르면 그 종목, 손익 칸에 ⇅ 없음, 머리 줄 48dp. 켜져 있으면 두 시장 칩('미국 주간거래 · 한국 휴장', 원화 보유액이 큰 시장부터, 보이는 시장 중 장중인 시장이 있을 때만 금색), '보유 17 · 관심 1', 지수 줄 계좌 비중 순서(원/달러는 끝·등락률, 지난 세션 값은 흐리게 + 날짜 — 좁으면 둘째 지수부터 빼고 지수는 등락률만 남겨 시장마다 하나와 원/달러는 늘 보임, 4x3 이상은 두 줄까지), 종목 줄 '수익 …'(합계와 같은 통화, 기본 원화 — 설정 '위젯 종목 금액')·'오늘 …', ⇅ 와 '누적'/'오늘', ↻ 를 합계 줄로 옮겨 같은 크기에 종목 줄이 더 보임(4x2 330×230dp 약 2.5 → 3.5줄). 종목 줄(약 38dp)은 예전처럼 누르면 그 종목 상세를 엶. 서버는 새 앱이 `&ui=2` 로 물을 때만 칩의 시장별 문구(와 그 세션 경계를 본 `nextChangeAt`)와 지수 다섯 개(코스닥·S&P500 추가)를 넣고, 끄면 넣지 않음(칩은 예전 앱과 같은 값). 앱 fallback 은 `false`(예전 서버는 예전 모습), 설정 화면의 '위젯 종목 금액'도 켜져 있을 때만 보임 폴드 안쪽 화면처럼 넓게 늘린 위젯(3-42)도 이 플래그를 따름: 켜져 있으면 잔고는 한 열 300dp 이상이면 종목 두 열·480dp 이상이면 평가금액 칸, 지수·환율은 폭 540dp 이상이면 구역 안 칸을 옆으로(끄면 예전 한 열) |
| `widgetExtended` | 켜짐 | (위젯 리뷰 1) 미국 프리마켓·애프터마켓·주간거래처럼 달력으로는 닫혀 있어도 보유 종목이 거래되는 세션이면, 서버가 `/api/widget` 칩에 시장별 연장 세션 표시(`market.ext` — 세션이 열려 있고 거래 대상으로 확인된 종목이 있을 때만, 주간거래 미지원·모름이면 false)를 넣고 새 앱 위젯이 장중처럼 15분마다 갱신(백그라운드 작업이 건너뛰지 않고, 위젯 주기·크기 변경 갱신도 받아 둔 응답을 15분까지만 다시 씀)하며 그 시장 시세가 30분 넘게 묵으면 '지연'. 끄면 칩에 `ext` 를 넣지 않고 `features.widgetExtended` 가 false 라 예전처럼 휴장 규칙(최대 2시간 재사용, 지연 없음). `ext` 는 세션 칩을 묻는 앱(`&sessions=1`)에만 넣고 칩의 다른 칸은 그대로라 예전 앱(1.4.0 지금 JS 포함)은 모르는 칸으로 무시함. 위젯에는 `/api/widget` 의 `features` 로 전하고 앱 fallback 은 `false`(예전 서버는 예전 규칙). 앱이 위젯을 바로 그릴 때도 잔고 시세의 세션으로 같은 규칙(공용 픽스처 `shared/fixtures/widgetExtended.json`). `ext` 와 그 '지연' 판단은 **보유 종목(수량 > 0)만** 봄 — 관심 종목만 프리마켓이면 켜지지 않고, 연장 세션 시장의 '지연'은 보유 종목 시세로만 따짐(달력으로 열린 정규장은 예전처럼 모든 종목, 서버·앱 같은 조건). 서버 호출은 미국 주식 보유자만 연장 세션 시간에 하루 약 20회 늘어남 |
| `widgetFoldFit` | 켜짐 | (위젯 2차, 앱만 — 서버는 `/api/widget` 의 `features` 로 전하기만 함) 접는 폰의 바깥 화면(접힘)에 펼친 화면 모양이 나오지 않게 함. ① 기기 규칙(위젯 번호와 상관없음): 이 기기가 짧은 변 600dp 이상 화면을 보인 적이 있으면(기기 AsyncStorage `widget.frame.device` — 90일) 접는 폰으로 보고, 지금 화면의 짧은 변이 600dp 미만(바깥 화면, 앱의 넓은 창 기준과 같음)이면 넓은 모습(잔고 평가금액 칸·두 열, 지수·환율 구역 안 옆 칸·큰 값 글자)을 쓰지 않음. ② 위젯마다 '어느 화면(짧은 변)·어느 방향에서 어떤 크기를 봤는지'를 화면·방향마다 한 줄씩(최대 4줄) 기기 AsyncStorage `widget.frame.<위젯 번호>` 에 적고(서버로 보내지 않음), 바깥 화면에서 본 적 있고 지금 폭이 그 화면에 들어가면 넓은 모습 없음, 두 화면에서 본 적 있으면 넓은 모습은 가장 좁은 폭이 허락할 때만. 기억은 홈 화면이 알려 준 크기(크기 변경 알림·추가)만 바꾸고, 그 밖의 그리기는 없던 줄만 더하거나 하루 넘게 같은 크기를 못 본 줄만 고침(접은 뒤 홈 화면을 열기 전 옛 옵션 크기로 그려져도 기억을 잃지 않게), 14일 못 본 줄은 버림, 위젯을 지우면 지움. 칸 크기는 그대로(투명한 빈 곳 없음). 일반 폰·태블릿은 지금 그림 그대로. 설정 '화면 정보' 공유 글 끝의 위젯 진단(번호·크기·기억)은 플래그와 상관없이 붙음(사용자가 공유할 때만, 서버로 보내지 않음). 끄면 기억을 읽지도 적지도 않고(지우기만) 지금 그림 그대로. 앱 fallback 은 `false`(예전 서버는 지금 그림) |
| `widgetFoldBoth` | 꺼짐 | (켜면, 위젯 2차 실험 — `widgetFoldFit` 도 켜져 있어야 함) 한 그림을 두 화면이 같이 쓰는 폴드 홈 화면(① 범위 — 두 화면에서 같은 방향·같은 크기를 봤거나, 모양으로 가릴 수 없고 두 화면 모두에서 같은 크기 알림이 온 적 없음)으로 보이는 위젯을 두 화면에 모두 들어가는 카드(본 것 중 작은 폭 × 작은 높이, 남는 오른쪽·아래는 투명)로 그림. 잘림은 없어지지만 안쪽 잔고 칸의 약 36%(3줄)만 카드이고, 두 화면에 따로 놓은 안쪽 위젯도 세로·가로 크기가 다르면 이렇게 보여 칸이 빔(예: 777×352 칸에 587×352 카드, 76%). 그래서 기본은 꺼 두고 설정 '화면 정보' 공유 글로 ①임을 확인한 뒤에만 켬. 꺼져 있으면 칸 크기 그대로. 앱 fallback 은 `false` |
| `widgetRefreshLog` | 켜짐 | (위젯 리뷰 2) 설정 > 표시 > '홈 화면 위젯 갱신' 아래의 자동 갱신 기록 요약('마지막 자동 갱신 10:47 · 오늘 평균 간격 18분 · 실패 1회'), 장중(한국 08:00~20:00·미국 정규장)에 1시간 넘게 자동 갱신이 없을 때 경고, '이 앱 설정 열기' 버튼이 사라짐. '마지막 자동 갱신'은 백그라운드 작업·위젯 주기 갱신이 서버에서 받아 그린 때, '평균 간격'은 이 둘이 **서버에 실제로 물은** 사이(성공·실패 — 두 시장이 닫혀 건너뜀·받아 둔 응답 재사용·앱 즉시 갱신·↻ 는 세지 않음). 경고는 그 한 시간 동안 자동 갱신이 돌긴 했는데 실패만 했으면 절전 탓이 아니라 실패 사유('갱신 실패 · 연결 안 됨' 등)와 연결 확인을 안내하고, 기록 자체가 없을 때만 절전 안내. 버튼은 이 앱의 정보 화면(`Linking.openSettings` — 거기서 배터리 → 제한 없음, 안내 글도 그 화면 말로)을 먼저 열고, 안 열리면 배터리 최적화 목록. 기록 자체(백그라운드 작업 성공·건너뜀·실패, 위젯 주기 갱신 성공·재사용(건너뜀)·실패, ↻·앱 즉시 갱신)는 플래그와 상관없이 이 기기 AsyncStorage `widget.refreshLog`(3일치, 안전장치로 최대 1000줄 ≈ 45KB)에만 적고 서버로 보내지 않음. 앱에서만 쓰는 플래그라 서버 작업은 없음. 앱 fallback 은 `false` |
| `allocationView` | 켜짐 | 잔고 탭 계좌 평가의 '비중' 버튼이 사라지고, 비중 화면을 열어도(남아 있던 화면 등) 잔고를 받거나 계산하지 않고 짧은 안내만 보임. 앱에서만 쓰는 플래그라 서버 작업은 없음. 앱 fallback 은 `false`(서버가 켜 준 뒤에만 버튼이 보임) (비중 보기) |
| `accountBriefing` | 켜짐 | 계좌 한 장 브리핑(3-31)을 만들지 않음(시세·지수 조회·모델 호출 0). 세션 알림은 예전 문구('오후 브리핑 17종목'), 브리핑 탭의 '내 계좌 브리핑' 카드와 상세 화면이 사라지고, `/api/notifications/settings` 의 `accountBriefing` 이 false 이고 `/api/widget` 응답에 `accountIds` 가 없어 앱 백그라운드 알림도 계좌 브리핑을 묻지 않음. 앱 fallback 은 꺼짐(예전 서버는 카드 없음). 계좌 요약은 `briefingDigest` 가 켜져 있을 때만 세션 알림 앞머리로 들어감 |
| `accountBriefingLlm` | 꺼짐 | (켜면) 계좌 브리핑의 설명 문단을 모델이 씀(3-31, 실험). 모델 설명은 입력에 없는 숫자·부호·권유/전망 표현 검사를 거치고 걸리면 기본 설명으로 바꾸지만, 자유 문장을 완전히 막을 수는 없어 기본은 꺼 둠. 꺼져 있으면 설명도 숫자로 만든 기본 문장이고 계좌 브리핑의 모델 호출 0 |
| `foldLayout` | 켜짐 | 접는 폰·넓은 창 화면(3-42)이 모두 꺼지고, 창 크기와 상관없이 지금 휴대폰 화면 그대로 보임(탭은 아래, 탭 머리 그대로). 켜져 있어도 접은 화면·폭 600dp 미만 창은 휴대폰 화면 그대로이고(종목·지수 상세 차트 크기도 예전 식 min(창 폭 − 56, 720) — 사용자 결정 2026-09-26. 단 `detailPolish` 가 켜져 있으면 폭만 잰 폭, 아래 `detailPolish` ③), 폭 600dp 이상 '넓은 창'에서만 아래가 바뀜 — **공통 틀**: 네 탭(잔고·발견·브리핑·설정) 모두 탭 화면 머리를 숨기고 화면이 상태 표시줄·좌우 여백을 둠. 넓고 낮은 창(폭 840dp 이상·높이 760dp 미만, 펼친 폴드8 가로)은 탭을 왼쪽 세로 막대로(탭 4개를 세로 가운데에). 가운데로 모으는 읽기 폭은 주 화면에 쓰지 않음(사용자 결정 2026-09-25). **잔고**: 맨 위 띠(지수 두 줄 칸 · 시장 상태 두 줄 · 검색) + 계좌 띠(한 줄 52dp, 폭 800dp 미만이면 두 줄) + 44dp 넓은 표(숫자 열은 폭·글자 크기에 따라 최대 8칸, 열 머리를 누르면 정렬). 미국 종목 현재가·평단·전일대비는 늘 달러, 평가손익·당일손익·평가금액 열만 '해외주식 원화 표시'를 따름. 접고 펼 때 맨 위에 보이던 종목을 이어 봄. **종목·지수 상세**: 차트 | 숫자 좌우(펼친 폴드8 가로·울트라 펼침 가로), 윗줄+아랫줄(울트라 펼침 세로), 한 단(펼친 폴드8 세로). 상세 머리의 ‹ n/17 › 로 잔고 정렬 순서대로 다음 보유 종목, 뒤로 1번이면 잔고에서 마지막에 본 줄을 2.4초 강조. **브리핑**: 2단(왼쪽 목록 400dp + 오른쪽 본문 — 펼친 폴드8 가로·울트라 펼침), 카드 격자(펼친 폴드8 세로), 넓은 창 브리핑 상세 두 칸, 계좌 브리핑 상세 두·세 칸. 켜져 있을 때만 연 브리핑 id 를 기기 AsyncStorage `briefings.read`(최근 300개)에 적어 넓은 창 목록의 미확인 점·처음 고를 브리핑에 씀(꺼져 있으면 읽지도 적지도 않음). **발견**: 시장·분류 한 줄(오른쪽 끝 검색), 순위는 44dp 한 줄 표(열 수는 폭에 따라), 테마 목록 두 칸·히트맵 칸 수 = 폭 ÷ 150. **설정**: 카드 두 칸(한 칸 340~400dp, 글자 크기와 상관없음 — 좁은 칸에서는 이름·값 줄의 값이 이름 아래로), 폭 600~687dp 창은 한 칸. **비중**: 카드 2×2, 원 옆에 범례. 기준선에서는 켜기·끄기 여유 24dp(2단 840dp 에서 켜고 816dp 아래로 좁아져야 끔, 왼쪽 막대도 폭 816dp 아래·높이 784dp 이상이 되어야 아래로, 설정·발견·비중 칸 수도 같은 여유)를 두어 창 크기를 끌어 바꿀 때 깜빡이지 않고, 접은 화면에서 펴면 처음 연 것과 같은 배치. 기준 폭·높이는 폰 실측 전 추정값이라 앱 `tokens.ts` 의 `layout` 에서 바꿈. 앱에서만 쓰는 플래그라 서버 작업은 없음(서버에는 설명 글만). 앱 fallback 은 `false`(예전 서버는 휴대폰 화면 그대로). 켜져 있으면 휴대폰·접은 화면에서도 모양은 그대로이고 보이지 않는 기록만 가볍게 함: 잔고 맨 위 종목 이어 보기(스크롤 위치 0.1초 간격·줄 위치 재기, 켜지는 순간 목록을 한 번 새로 그림), 종목 상세 기간·탭을 주소 검색어에 적기(`router.setParams` — 접고 펴도 이어 보게), 연 브리핑 읽음 기록(`briefings.read`)·고른 브리핑 기억(모듈 저장소), 브리핑 탭 머리 옵션 다시 적기(Tabs `setOptions` 로 `headerShown: true` — 보이는 값 그대로). 꺼져 있으면 이 기록도 모두 하지 않음. 넓은 창 차트: 기간 칩·과거로/최신으로/크게 보기·이동평균 스위치는 누르는 영역 44×44, 차트 아래 이동평균 값 줄은 항목('■ 120일 77,120원') 단위로 줄바꿈, 옆으로 넘기는 칩 띠는 넘길 칩이 있는 쪽 끝을 바탕색으로 흐리게(휴대폰·접은 화면은 모두 예전 그대로 — 단 칩 띠 끝 흐림은 2026-09-26 버그 수정으로 이 플래그와 상관없이 모든 창에 적용, 아래 `detailPolish` 줄 참고). 탭 이름을 늘 아이콘 아래에 두는 것(폭 768dp 이상에서 옆으로 가던 것)과 조 단위 금액의 천 단위 쉼표, 전체 화면 차트 머리(늘 예전처럼 한 줄로 그려 잰 뒤, 넘칠 때만 이름을 '…'로 줄이고 그래도 이름이 네 글자도 남지 않으면 가격·등락을 둘째 줄로 — 다 들어가면 글자 크기·높이 44 까지 예전 머리 그대로, 글자를 150% 까지로 묶는 것은 두 줄 머리만)는 버그 수정이라 이 플래그와 상관없이 적용 탭 위치: 2026-09-26 사용자 선택으로 펼쳐도 아래 탭 바(왼쪽 세로 막대는 `tokens.ts` layout.railOn 으로 꺼 둠, 켜면 넓고 짧은 창만 막대). |
| `detailPolish` | 켜짐 | 종목 상세 다듬기(2026-09-26 사용자 RGTX 접은 화면 캡처, 사용자 승인으로 접은 화면에도 적용). 끄면 아래 네 가지가 사라지고 예전 화면 그대로: ① 휴대폰·접은 화면(폭 600dp 미만 또는 `foldLayout` 꺼짐) 시세 머리 맨 아래 **보유 한 줄** `보유 160주 · 평가손익 -2,342,254원 (-26.25%)` — 잔고 화면 줄과 같은 평가(매도 비용 차감·해외주식 원화 표시 설정을 따름), 색은 보이는 값의 부호, 화면 읽기는 한 문장, 한 줄(글자 100% 초과면 두 줄까지). 이 줄이 있을 때만 시세 머리 위아래 여백을 12 → 8dp 로 줄이고, 달러 종목은 환산·환율 줄과 시세 기준·시각 줄을 한 줄로 합침(`$10.61 · 환율 1,391.50 · TOSS · 9월 26일 (토) 05:00` — 시세 머리 줄 수가 그대로라 폴드8 접은 화면 앱 영역 475×663·글자 115% 에서도 차트 아래 이동평균 칩 줄이 밀려나지 않음. 원화 종목은 합칠 줄이 없어 한 줄 늘어남). 보유 수량과 평가가 있을 때만(관심·미등록·평단 없음은 없음). 넓은 창은 옆 칸·첫 칸 '내 보유'에 이미 보여 그리지 않음 ② 차트를 과거로 옮기면(드래그·‹ 버튼) 차트 위 조작 줄의 ‹ 바로 앞에 **`2일 전까지 보는 중 · 최신으로`** 버튼 — 차트 그림 위에 덮지 않아 봉·글자를 가리지 않고, 그림·가격 축은 그대로라 버튼이 생겨도 축이 튀지 않음(2026-09-26 이전 버전은 그림 맨 위에 떠서 가격 칸 위쪽 40dp 를 비웠음). 몇 일 전인지는 버튼이 보여 주므로 봉 수 칩은 `120일` 만 적음. 버튼을 넣고 칩 띠에 일·주·월 칩이 보일 폭(120dp)이 남지 않으면 짧은 글 `2일 전 · 최신으로`(접은 화면 411·360, 475 에서도 글자 115%·월봉 `30개월 전` 등). 단위: 일·주·개월(봉 수 칩도 월봉은 `120개월 · 30개월 전`), 분봉은 같은 날 안이면 `15분 전`·`1시간 5분 전`, 다른 날까지 가면 보이는 마지막 봉의 `9월 23일 14:05까지`. 누르면 최신 구간으로, 누르는 틀 자체가 높이 44dp(보이는 버튼 32dp — 틀 밖 hitSlop 에 기대지 않고, 위아래 음수 여백으로 조작 줄 높이는 그대로), 화면 읽기는 늘 긴 글, 종목·지수 상세와 전체 화면 차트 모두. ③ 휴대폰·접은 화면(넓은 창 배치가 아닌 창)도 **차트 폭은 차트 칸이 실제로 받은 폭 전부** — 예전 식 min(창 폭 − 56, 720)은 오른쪽에 28dp 빈 띠가 남았음(475 창 419 → 447dp, 411 355 → 383, 360 304 → 332, 720 상한은 그대로). 높이는 예전 그대로(예전 폭 × 0.62) 라 차트 아래 칩 줄·고지 줄 위치는 바뀌지 않음(2026-09-26 사용자 요청 '차트 부분 오른쪽 빈 공간 여백 없애줘') ④ **맞춘 가격 축**: 축 글자(눈금·현재가 태그·십자선 값·거래량 최댓값·RSI 30/70)를 그림 오른쪽 끝에서 2dp 안쪽에 오른쪽 맞춤으로 적고, 축 칸은 가장 긴 글자 + 6dp 만큼만(예전은 축 칸 왼쪽 + 4 에 왼쪽 맞춤, 글자 폭을 넉넉히 어림해 글자 오른쪽이 약 8dp 비었음). ③④ 로 475 접은 화면의 봉 칸이 약 30dp 넓어짐. 보이는 구간은 화면 상태라 화면을 다시 열거나 기간을 바꾸면(일 → 주 → 일로 돌아와도) 최신 구간·기본 봉 수에서 시작. 앱에서만 쓰는 플래그라 서버 작업은 없음. 앱 fallback 은 `false`(예전 서버는 예전 화면). **플래그와 상관없는 버그 수정(같은 날):** 차트 한 손가락 드래그(과거/최신 이동)는 가로로 14dp 움직여야 시작하고, 그 전에 세로로 10dp 움직이면 포기해 화면 스크롤로 넘김(예전에는 어느 쪽이든 8dp 에 시작해, 세로로 스크롤하던 손가락이 옆으로 조금 흔들리면 차트가 과거로 옮겨졌음 — RGTX 캡처의 `120일 · 2일 전`), 시작하는 순간까지 움직인 14dp 는 이동에 넣지 않음(봉이 한꺼번에 튀지 않게, 두 손가락 확대·길게 눌러 십자선은 그대로), 볼린저 밴드 채움이 가격 칸 위로 벗어난 음수 좌표를 빠뜨려 모양이 틀어지던 것, 가격 축은 봉(+최신 구간이면 현재가, ±25% 안쪽 평단) 기준 — 이동평균·볼린저 선은 봉 범위 폭의 10% 까지만 축을 넓히고 넘는 부분은 가격 칸에서 잘라 그림(크게 떨어진 종목의 120일선 옛 값이 봉을 바닥에 눌러 두던 것 — 단 보이는 구간에 급등한 봉이 있으면 축은 그 봉까지 그대로라 최근 봉은 여전히 낮게 보임, 60일 칩·확대로 볼 것), 보이는 구간 내내 가격 칸 밖으로 벗어난 이동평균은 가장자리에 `120일선(범위 위)` 표시(그 선 색 네모 + muted 글자), 평단·52주 글자는 옅은 바탕 상자와 함께 선 위·아래를 가로로 훑어 봉·다른 글자·현재가선을 가리지 않는 자리(최신 봉 5개는 덮지 않고, 현재가선을 끊는 자리는 지난 봉 몇 개를 덮는 자리보다 피함 — 오늘 52주 신저가처럼 52주 최저선이 바닥에 붙고 현재가선이 바로 위면 가격 칸 아래 틈(거래량 칸 맨 위 2dp 까지)을 써서 선 아래에. 최신 봉·현재가선을 덮을 수밖에 없거나 봉이 빽빽해 8개 이상 덮으면 52주·이동평균 글자는 빼고 선만 — 평단은 늘 적음), 선을 모두 그린 뒤 글자를 그림(현재가 점선이 글자를 가로지르지 않게), 옆으로 넘기는 칩 띠 끝 흐림은 모든 창(잘린 '30분'·'RSI' 칩이 깨져 보이던 것 — 흐림에 꺾쇠 ›는 두지 않음: 잘린 칩 글자와 겹쳐 'RSI/M›'로 뭉개지고 누르면 밑의 칩이 눌렸음), 가격 칸 자르기 틀 id 는 차트마다 따로(웹 미리보기에서 두 차트가 한 틀을 쓰던 것), 업종 자리표시('-'·'N/A')는 적지 않음, 이름이 티커뿐인 종목(토스 동기화로 들어온 RGTX 등)은 제목은 티커 그대로 두고 서버가 새로 주는 `quote.fullName`(네이버 stockName — 한글 이름이 있으면 한글, ETF 는 영문)을 부제목 끝에(`RGTX · NASDAQ · Defiance Daily …`, 없으면 붙이지 않음). **서버 변경은 칸 추가만:** `quote.fullName` 새 칸. `quote.industry` 는 예전 그대로(ETF 등은 '-' 로 옴) 두고 앱이 자리표시를 거름(상세 부제목·비중 화면은 '-' 대신 적지 않음·'업종 정보 없음') |

위젯 플래그 일곱 개(`widgetPnlToggle`·`widgetIndexLine`·`widgetMarket`·`widgetPolish`·`widgetExtended`·`widgetFoldFit`·`widgetFoldBoth`, `widgetRefreshLog` 는 앱 설정 화면만이라 빼고)는 홈 화면 위젯이 `/api/features` 를 따로 받지 않으므로 `/api/widget` 응답의 `features` 칸으로도 받습니다(위젯이 쓰는 것만).
응답에 `features` 가 없거나 키가 없으면(예전 서버) 위젯은 그 기능을 꺼진 것으로 봅니다(fallback `false`). 지수·환율 위젯은 이때 짧은 안내만 보입니다.
앱이 떠 있을 때 위젯을 바로 다시 그리면, 앱이 받은 `/api/features` 값과 위젯이 받아 둔 `/api/widget` 값 중 **받은 시각이 늦은 쪽**을 씁니다.
앱의 플래그 캐시는 기기에 며칠 남을 수 있어서(브리핑·설정 화면을 열 때만 다시 받음), 늘 앱 값을 쓰면 끈 기능이 앱을 열 때마다 되살아나기 때문입니다.
위젯이 스스로 갱신할 때도(받아 둔 `/api/widget` 응답을 장중 15분·휴장 2시간까지 다시 쓰거나, 조회에 실패했을 때) 같은 규칙입니다. 마지막으로 그린 값(앱이 적은 것)이 그 응답보다 늦게 받은 것이면 그쪽을 씁니다. 그래서 앱 갱신과 위젯 갱신이 번갈아도 전환 칸·지수 줄이 켜졌다 꺼졌다 하지 않습니다. ↻로 서버에서 방금 받은 값은 늘 그대로 씁니다.
잔고·칩·기준 시각도 같은 규칙인데, 견주는 시각은 **잔고를 받은 시각**입니다(앱은 잔고 쿼리의 react-query `dataUpdatedAt`, 위젯에 넘긴 시각이 아님). 시세 시각(가장 늦은 `asOf`)도 뒤지지 않아야 합니다. 그래서 앱이 다른 탭에 있다가 떠날 때 넘긴 09:48 잔고는 백그라운드 작업이 10:00 에 받은 잔고를 이기지 못하고(넘기는 순간에도 10:00 값을 둠), 조회 실패 때 쓰는 마지막 잔고도 덮지 않습니다. 더 새 기록에 칩이 없으면(앱이 장 상태를 받기 전) 응답의 칩을 둡니다. 칩과 플래그가 서로 다른 기록에서 오면 연장 세션 표시(`ext`)를 고른 플래그로 다시 거릅니다.

`widgetPolish` 가 켜져 있으면 위젯이 이것도 합니다 (위젯 검토 7번, 앱 OTA 만 · 서버 변경 없음). 끄면 아래 셋이 모두 지금처럼 돌아갑니다.
- **브리핑 위젯 바로 갱신**: 앱이 잔고를 위젯에 넘길 때(앱을 열거나 떠날 때, 시세만 바뀌면 1분에 한 번) 앱이 받은 최신 브리핑 목록도 넘겨 브리핑 위젯을 바로 다시 그립니다. 3종목은 서버 `/api/widget` 과 같은 규칙(성공한 것만, 원화 환산 보유 비중 순 → 같으면 최신 순)으로 고르고, 위젯이 받아 둔 응답보다 늦게 받은 목록일 때만 씁니다. 목록이 바뀌면(다시 만들기·새 브리핑) 1분을 기다리지 않고, 시세 때문에 순서만 바뀌면 1분 규칙을 따릅니다.
  - 앱은 목록을 스스로 처음부터 받지 않습니다. 브리핑 탭이 받은 것을 쓰고, 누가 목록을 무효화하면 브리핑 탭이 가려져 있어도 한 번 다시 받습니다. 무효화하는 때는 다시 만들기·브리핑 알림(받거나 누름)·종목 등록·수정·삭제입니다. 켜져 있으면 이때마다 `/api/briefings/latest` 가 한 번씩 늘고(수정은 목록이 같아 넘기지 않음), 끄면 늘지 않습니다.
  - 잔고를 이번 실행에서 받지 않았을 때(위젯의 종목 브리핑·알림을 눌러 앱을 새로 켜 브리핑 상세로 바로 들어감 — 잔고 탭이 가려져 잔고를 받지 않음)는 브리핑 위젯만 다시 그립니다. 잔고·자산·지수 위젯과 저장된 잔고·칩·기준 시각은 그대로 두고(기기 저장값 잔고로 위젯을 덮지 않는 3-16 규칙), 3종목은 위젯이 마지막으로 그린 잔고로 고릅니다.
  - 앱이 넘긴 브리핑은 위젯이 받아 둔 `/api/widget` 응답에도 적습니다(브리핑과 앱 목록을 받은 시각 `briefingsAt` 만 — 받은 시각·ETag·잔고·지수·알림용 id 는 그대로, 적는 곳은 저장 키 주인인 `widgets/data.ts`). 그래서 위젯이 스스로 갱신할 때(주기 갱신·폴드를 접고 펼 때의 크기 변경 — 받아 둔 응답 재사용, 조회 실패)도 새 브리핑이 옛것으로 되돌아가지 않고, 그보다 먼저 받은 앱 목록(메모리에 남은 옛 목록)도 다시 덮지 않습니다. 서버가 새 응답을 주면 서버 목록으로 바뀝니다.
  - 끄면: 브리핑 위젯은 백그라운드 작업·위젯이 스스로 받은 것만 그리고, 받아 둔 응답도 건드리지 않습니다.
- **브리핑 위젯 제목·안내 문구**: 제목(48dp 머리 줄)과 안내 문구('브리핑 생성 실패 N종목 …'·'아직 브리핑이 없습니다 …' — 그 아래 남는 곳까지)를 누르면 브리핑 탭이 열립니다. 화면 읽기는 안내 문구와 고지 문구를 함께 읽고, 안내가 한 줄뿐인 크기(4x1·큰 글자)에서는 문장 끝을 '…'로 줄입니다. 조회 실패 안내('… ↻ 로 다시 시도')는 켜져 있어도 누르는 칸이 아닙니다. 끄면: 제목은 잔고 탭, 안내 문구는 누르는 칸이 아님.
- **잔고 위젯 지수 줄**: 항목을 누르면 그 지수·환율 차트(`market/코드`, 지수·환율 위젯 칸과 같은 곳)가 열립니다. 지수 줄이 두 줄이어도(4x3 이상·폴드8 커버 4x2 크게) 항목마다 따로입니다. 항목이 48dp 보다 좁을 때만 줄 전체가 첫 항목의 차트 한 칸입니다(차트 화면 위 띠에서 다른 지수로 바꿀 수 있음). 끄면: 줄 전체가 잔고 탭.
- 폰에서 확인할 것: 위젯 라이브러리(네이티브, OTA 로 못 고침)가 누르는 칸마다 만드는 연결(PendingIntent)이 같은 1ms 안에 만들어지면 앞 칸이 풀릴 수 있습니다. 지수 줄 항목과 브리핑 안내 칸을 하나씩 눌러 맞는 화면이 열리는지 봅니다.

지수는 지수 줄을 그릴 수 있는 새 앱만 받습니다. 새 앱은 `/api/widget?indices=1` 로 묻고, 서버는 이 표시가 있을 때만 지수를 넣습니다(지수 조회도 그때만).
예전 앱(1.3.0)은 쿼리 없이 물으므로 지수 없이 받습니다. 한국 종목만 가진 사용자가 장이 닫힌 뒤에도 나스닥·환율이 바뀌어 304 대신 200 을 받지 않게 하려는 것입니다. 예전 서버는 모르는 쿼리를 무시합니다.

지수·환율 위젯의 판 9개(코스피·코스닥·나스닥·S&P500·다우·필라반도체·원/달러·원/100엔·원/위안)도 같은 규칙입니다.
- 1.4.0 앱은 홈 화면에 지수·환율 위젯이 있을 때만(백그라운드 작업), 또는 그 위젯이 스스로 갱신할 때 `/api/widget?indices=1&board=1` 로 묻습니다. 위젯이 없는 사용자의 응답·ETag 는 예전과 같습니다.
- 지수 줄과 판은 앱 지수 띠(`/api/market/indices?stale=1`)와 같은 목록(같은 30초 캐시·stale 규칙)에서 나오고, 둘을 함께 물어도 지수 출처는 한 번만 부릅니다.
- 앱이 떠 있으면 앱 지수 띠 값으로 바로 다시 그립니다(받은 시각이 늦은 쪽). 조회가 실패하거나 판 없이 받은 응답(다른 위젯의 ↻)이어도 마지막 판을 그대로 두고, 받은 지 3시간이 넘으면 모든 칸을 '지연'으로 그립니다.
- 서버가 `widgetMarket` 을 꺼 두었거나 모르면(롤백한 서버 등 — 물어도 판을 주지 않음) 위젯의 주기·추가·크기 변경 갱신은 받아 둔 응답을 다시 씁니다(다른 위젯과 같은 재사용 규칙, 판을 달라고 계속 묻지 않음).
- 받아 둔 `/api/widget` 응답이 아직 없을 때(업데이트 직후) 첫 조회가 실패하면, 마지막으로 그린 값(앱이 적은 것)의 플래그·지수를 그대로 씁니다. 실패했다고 플래그를 꺼짐으로 보지 않습니다(손익 전환 칸·지수 줄도 같음).

`widgetPnlToggle` 이 켜져 있어도 위젯이 낮아 목록 첫 줄이 들어갈 자리가 없으면, 그 크기에서는 전환 칸(48dp)을 빼고 누적만 보여 줍니다(누르면 앱, 끈 것과 같은 모습). 흔한 4x2(높이 150dp 이상)에서는 목록과 전환 칸이 함께 보입니다.
- 알려진 한계: 4x2 최소 높이(110dp, 100·130%)와 250×130dp(100%)처럼 낮은 크기에서는 손익을 눌러도 앱이 열리고, 위젯에서 당일 손익을 볼 수 없습니다(2x1 자산 위젯의 '오늘'이나 앱에서 봅니다). 머리 줄 48dp + 전환 칸 48dp + 아래 여백이 위젯 높이를 넘거나, 넣으면 목록이 사라지기 때문입니다. 누르는 칸을 합계 줄 위로 겹치는 방법은 실기기에서 누르는 곳이 겹치지 않는지 확인이 필요해 이번에는 하지 않았습니다.
- 손익 칸 설정(누적·당일)은 잔고 위젯 모두가 같이 씁니다. 한 위젯에서 바꾸면 다른 잔고 위젯도 바로 같은 쪽으로 다시 그립니다. 누른 위젯은 늘 보여 주던 쪽의 반대로 바뀝니다(누르는 칸이 보여 주는 쪽을 함께 보냄).

## 바꾸는 법

```bash
# 목록 (기본값·설명·바꾼 값)
curl -H "Authorization: Bearer $API_TOKEN" https://<서버>/api/admin/features
# 끄기 / 켜기 / 기본값으로 되돌리기(null)
curl -X PUT -H "Authorization: Bearer $API_TOKEN" -H "content-type: application/json" \
  -d '{"tossReconcile": false}' https://<서버>/api/admin/features
```

- 서버는 바로 반영하고(바꾼 값은 DB `meta.features` 에 저장, 재시작해도 유지), 앱은 1분 안에 반영합니다.
- 앱은 마지막으로 받은 값을 기기에 저장해, 다음에 켤 때(오프라인 포함) 그 값으로 시작합니다.
- 앱이 아직 한 번도 못 받았거나(첫 실행·예전 서버) 서버가 모르는 플래그는 `useFeature(키, fallback)` 의 fallback 을 씁니다. 새 기능은 `false`(서버가 켤 때만 보이게), 이미 나간 기능을 감싼 플래그는 `true`.
- 서버는 값을 30초 캐시하고(여러 인스턴스·DB 직접 수정도 따라가게), DB 를 못 읽으면 꺼짐으로 봅니다. 앱은 30초마다, 앱으로 돌아올 때마다 다시 받습니다.
- 관리 API 는 앱과 같은 `API_TOKEN` 으로 보호됩니다(다른 `/api/admin/*` 와 같음).

예외: 설정의 '화면 정보' 카드(3-42 1단계, 접는 폰 측정용)는 사용자 요청으로 플래그 없이 넣었습니다. 화면 표시만 하고 서버 작업이 없습니다.

## 새 플래그 추가

1. `FEATURES` 에 `{ default, description }` 한 줄 추가
2. 서버 작업은 `features.enabled("키")`, 앱 화면은 `useFeature("키", fallback)` 과 `gated()` 로 감쌈 (화면 조건마다 따로 거르지 말고 값 하나로)
3. 끈 상태에서 화면·알림·서버 작업 0건 테스트 추가, 위 표 갱신
